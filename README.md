#Issue with configuration cache enabled with custom remote build cache implementation

To replicate the issue run the following command 2x:
```
gradlew --configuration-cache --build-cache --stacktrace help
```

